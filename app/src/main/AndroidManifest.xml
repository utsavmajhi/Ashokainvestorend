<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashokainvestorend">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".investpayactivity"></activity>
        <activity android:name=".alltransactionpage"
            android:parentActivityName=".homepage"/>
        <activity android:name=".profileactivity"
            android:parentActivityName=".homepage"/>


        <activity android:name=".invpoolexpand"
            android:parentActivityName=".homepage"/>
        <activity android:name=".apoolexpand_activity"
            android:parentActivityName=".homepage"/>
        <activity android:name=".registeringuser" />
        <activity android:name=".homepage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>